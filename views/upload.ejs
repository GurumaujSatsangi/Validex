<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Upload - TrueLens</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #ffe600;">
      <div class="container-fluid">
        <a class="navbar-brand fw-bold text-dark" href="/">
          <i class="bi bi-check-circle-fill"></i> TrueLens
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link text-dark" href="/upload">Upload</a></li>
            <li class="nav-item"><a class="nav-link text-dark" href="/providers">Providers</a></li>
            <li class="nav-item"><a class="nav-link text-dark" href="/runs">Runs</a></li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container mt-5">
      <h1 class="mb-5 text-center"><i class="bi bi-cloud-arrow-up"></i> Add Providers to TrueLens</h1>
      
      <div class="row g-4 mb-5">
        <!-- CSV Upload Card -->
        <div class="col-md-4">
          <div class="card shadow h-100">
            <div class="card-header bg-primary text-white text-center py-3">
              <i class="bi bi-filetype-csv" style="font-size: 3rem;"></i>
              <h5 class="mt-2 mb-0">Upload CSV</h5>
            </div>
            <div class="card-body d-flex flex-column">
              <p class="text-muted mb-3">Import multiple providers from a CSV file with pre-formatted data.</p>
              <form id="uploadForm" class="mt-auto">
                <div class="mb-3">
                  <label for="csvFile" class="form-label fw-bold">Select CSV File</label>
                  <input type="file" class="form-control" id="csvFile" name="file" accept=".csv" required />
                </div>
                <button type="submit" class="btn btn-primary w-100">
                  <i class="bi bi-upload"></i> Upload CSV
                </button>
              </form>
              <div id="uploadResult" class="mt-3"></div>
            </div>
          </div>
        </div>

        <!-- PDF Upload Card -->
        <div class="col-md-4">
          <div class="card shadow h-100">
            <div class="card-header bg-info text-white text-center py-3">
              <i class="bi bi-file-pdf" style="font-size: 3rem;"></i>
              <h5 class="mt-2 mb-0">Upload PDF</h5>
            </div>
            <div class="card-body d-flex flex-column">
              <p class="text-muted mb-3">Extract providers from scanned PDF documents using OCR technology.</p>
              <form id="uploadPdfForm" class="mt-auto">
                <div class="mb-3">
                  <label for="pdfFile" class="form-label fw-bold">Select PDF File</label>
                  <input type="file" class="form-control" id="pdfFile" name="file" accept=".pdf" required />
                </div>
                <button type="submit" class="btn btn-info w-100">
                  <i class="bi bi-upload"></i> Upload PDF
                </button>
              </form>
              <div id="uploadPdfResult" class="mt-3"></div>
            </div>
          </div>
        </div>

        <!-- Add by NPI Card -->
        <div class="col-md-4">
          <div class="card shadow h-100">
            <div class="card-header bg-success text-white text-center py-3">
              <i class="bi bi-person-plus-fill" style="font-size: 3rem;"></i>
              <h5 class="mt-2 mb-0">Add by NPI</h5>
            </div>
            <div class="card-body d-flex flex-column">
              <p class="text-muted mb-3">Fetch and validate a single provider using their 10-digit NPI number.</p>
              <form id="addNpiForm" class="mt-auto">
                <div class="mb-3">
                  <label for="npiInput" class="form-label fw-bold">NPI Number</label>
                  <input 
                    type="text" 
                    class="form-control text-center" 
                    id="npiInput" 
                    placeholder="1234567890" 
                    maxlength="10"
                    pattern="[0-9]{10}"
                    style="font-family: 'Courier New', monospace; letter-spacing: 2px;"
                    required
                  />
                </div>
                <button type="submit" class="btn btn-success w-100">
                  <i class="bi bi-search"></i> Fetch Provider
                </button>
              </form>
              <div id="npiResult" class="mt-3"></div>
            </div>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/upload.js"></script>
  </body>
</html>
